dependencies:
   post:
      - wget https://selenium-release.storage.googleapis.com/3.4/selenium-server-standalone-3.4.0.jar
      - java -jar selenium-server-standalone-3.4.0.jar:
            background: true
test:
  override:
  - mvn -Dtest=SelenideDemo test
