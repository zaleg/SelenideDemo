version: 2
jobs:
   test:
     override:
     - $RUN_COMMAND
workflows:
   version: 2
   scheduled-workflow:
     triggers:
       - schedule:
           cron: "05 23 * * *"
           filters:
             branches:
               only:
                 - master
     jobs:
       - test
