workflows:
   version: 2
   nightly:
     triggers:
       - schedule:
           cron: "10 0 * * *"
           filters:
             branches:
               only:
                 - master
     jobs:
       - test
